/**
 * 可选：若页面仍引用 `config.local.js`，可复制本文件为 `config.local.js`。
 * 当前画廊与相机 **默认不依赖** 本文件；数据在浏览器 localStorage（键见 `gallery-storage.js`）。
 */
window.ASCII_CAMERA_BIN_ID = '';
window.ASCII_CAMERA_API_KEY = '';
window.ASCII_CAMERA_JSONBIN_AUTH = 'master';
