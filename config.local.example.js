/**
 * 使用步骤：
 * 1. 复制本文件为 `config.local.js`（与 camera.html、gallery.html 同目录）
 * 2. 登录 https://jsonbin.io → 创建 Bin，初始 JSON 填：{"photos":[]}
 * 3. 在 API Keys 页面复制 **X-Master-Key**（Master Key）
 * 4. 把下面的占位符换成你的 Bin ID 与 Master Key
 *
 * 勿将含真实密钥的 config.local.js 提交到公开仓库。
 */
window.ASCII_CAMERA_BIN_ID = 'YOUR_BIN_ID';
window.ASCII_CAMERA_API_KEY = 'YOUR_X_MASTER_KEY';
